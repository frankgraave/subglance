# SubGlance — autonomous work loop handoff

Read first, rewritten last. Keep under 60 lines; it is read every run.

## Current state
2026-09-11. Last: SUB-52 (dashboard embedded in the binary), 7835c6b, Done.
Branch main, tree clean, pushed. SUB-22 and SUB-29 stay In Progress: SUB-22
needs a visual check on a second monitor (only Frank) and runs on demo data
until SUB-26; SUB-29's detail view does not exist yet on any width (SUB-53).

**Serving the UI**: the frontend builds into `internal/webui/dist` (NOT
web/dist); `internal/webui` embeds it with `//go:embed all:dist`. Its handler
is the catch-all route `/` in `routes()`, registered for every method via
`anyMethod` (""). Unknown page path -> index.html; unknown `/api` path -> JSON
404; missing hashed asset -> real 404; non-GET/HEAD -> 405. The committed
`internal/webui/dist/.gitkeep` keeps `go build` working without Node: do not
delete it, keep `emptyOutDir: false` in the bundler config. `make dist` =
web-build + build. `route.documented()` is why `/` is in the route table but
not in docs/openapi.yaml.

**API**: `Server.requireMonitor` (internal/api/monitors.go) is the single place
answering "does this monitor exist" for sub-resource reads; all four call it,
`sql.ErrNoRows` -> 404, anything else -> 500. A fifth must call it and join
`monitorSubresourcePaths` in monitor_subresource_test.go.
`GET /api/v1/monitors?heartbeats=N` embeds beats via
`store.RecentHeartbeatsForAll` — one window-function query, not N+1.

**Frontend in `web/`**: React 19, Tailwind v4, oxlint, vitest; CI job `web`
also smoke-tests a real binary. `src/styles/tokens.css` is the ONLY file
allowed a colour (tokens.test.ts enforces it). Theme: `[data-theme]` on <html>,
localStorage, pre-paint script in index.html duplicating src/theme/theme.ts —
allowed by SHA-256 hash in internal/webui's CSP, so editing it needs no other
change. `src/heartbeat/` = model.ts (pure geometry) + HeartbeatBar.tsx.
`src/monitors/` = model/types/format, Unknown, EmptyState, Led, MonitorRow+
MonitorTable (desktop), MonitorCard+MonitorCardList (phone), Dashboard.tsx
picks between them, demo.ts, Workbench.tsx — all presentational, SUB-26 feeds
them. **One breakpoint, 640px**, in src/layout/useMediaQuery.ts and
monitors.css; breakpoint.test.ts asserts they agree (rationale: DESIGN.md §13).
Component CSS imported from src/index.css, never a .tsx. App.tsx is a tabbed
workbench, not routing.

## Standing constraints
Go 1.26; short suite hermetic, network tests behind SUBGLANCE_TEST_NETWORK=1.
Never force-push. Commits in Dutch, code and docs in English. Fix lint
findings, never suppress. Run golangci-lint and web targets under
`systemd-run --scope -p MemoryMax=1500M` — 3.7GB host, an unbounded run took
the gateway down before.

## Hard-won lessons
- Long sessions cost ~3.3x per call. Finish one issue, stop.
- A failing test is a hypothesis. Verify a fix by breaking it and check HOW it
  failed: a build error proves nothing. Sabotage logic, not types.
- The terminal tool rejects a heredoc whose text merely resembles a long-lived
  watcher (the bundler's name in a comment did it): write such files through a
  python heredoc, or substitute a placeholder.
- git cannot un-ignore a file inside an ignored dir: `!dir/`, `dir/*`, `!dir/k`.
- Tailwind v4 needs `@theme inline`; plain `@theme` freezes one theme.
- Absence and zero are different facts on every read path.
- Enum values live in internal/store/migrations/0001_init.sql CHECK constraints.
- Helpers exist: seedMonitor, testServerWithDB, seedUser, doJSON, openTestDB,
  `contains` (validate_test.go — do not redeclare).
- openapi.yaml is drift-tested, but its prose goes stale silently — grep it for
  old wording, not just status codes.
- jsdom: no layout (HeartbeatBar needs an explicit `width`), no `matchMedia`
  (stub it), `import.meta.url` is an http URL.
- Every HeartbeatBar renders an sr-only table with a "Latency" header, so a
  loose getByText inside a card or row matches the wrong node.
- oxlint react(set-state-in-effect) fires on any setState in an effect;
  `useSyncExternalStore` reads an external value instead.
