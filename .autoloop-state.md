# SubGlance — autonomous work loop handoff

Memory between runs: read first, rewritten last. Keep it under 60 lines — it is
read every run, so length is a recurring cost. Delete what is no longer true
rather than appending; the durable log lives in Linear and git history.

---

## Current state

Last updated: 2026-09-10 12:45 UTC
Last completed: notification channels API — commit 4c2476e
Branch: main, working tree clean, everything pushed

Phase 1 backend is complete except for the OpenAPI spec (SUB-8 through SUB-18
are all done). CI covers race, lint, three-platform build, govulncheck.

Schema is at migration 0003. Routes are registered in internal/api/server.go
under read/write/admin helpers — a new route must be added to one of those, not
to the mux directly, or it ships unguarded.

## In progress

SUB-15 (REST API v1). The whole endpoint surface now exists: monitors
CRUD+PATCH, pause/resume, check-now, heartbeats, uptime, incidents, tokens,
users and channels. ONLY the OpenAPI spec is left, and the surface has stopped
moving — write it next.

Channels landed this run (/api/v1/channels, /api/v1/monitors/{id}/channels).
Storage only: nothing is delivered yet, there is no notification worker.

## Next up after that

1. Finish SUB-15: write the OpenAPI spec, then close it.
2. SUB-20 — design tokens. Values already decided in docs/DESIGN.md §2; copy
   them, do not re-invent them. Blocks SUB-46/47.
3. SUB-46/47 — dashboard states (stale connection, paused vs no-data). Both
   are designed in docs/DESIGN.md and mocked in docs/mockups/.
4. SUB-49 — apply SSRF checks to webhook delivery. Filed this run; blocked
   until a notification delivery layer exists.
5. SUB-48 — ETag/If-Match on PATCH. Low priority.

## Standing constraints

- Go directive stays at 1.25.0. CI pins Go 1.25.14 and both linter versions;
  bumping the directive means checking a golangci-lint release exists that was
  built with that Go.
- Short test suite must stay hermetic: `go test -short` may not touch the
  network. Network tests sit behind SUBGLANCE_TEST_NETWORK=1.
- Never `git push --force`, never rewrite history.
- Commit messages in Dutch. Code, comments and docs in English.
- Fix lint findings; do not suppress them.
- golangci-lint under `systemd-run --scope -p MemoryMax=1500M`: the host has
  3.7GB and an unbounded lint run has taken the gateway down before.

## Hard-won lessons (do not relearn these)

- Long sessions cost ~3.3x per call versus short ones. Finish one issue, stop.
- A failing test is a hypothesis, not a nuisance. Three "broken tests" so far
  were all real product bugs.
- Verify a fix by breaking it, and check HOW it failed: a build error proves
  nothing. Also avoid sabotage that deadlocks — swapping a tx.ExecContext for
  db.Writer.ExecContext inside an open transaction hangs the single-connection
  writer pool until the test timeout.
- Absence and zero are different facts on every read path ("no samples" is not
  0% uptime). Aggregates must store their sample count to stay mergeable.
- Enum values live in the schema CHECK constraints (migrations/0001_init.sql).
  Read them before validating in Go — keyword_mode is absent_ok/must_contain/
  must_not_contain, not what you would guess.
- Test helpers already exist: seedMonitor (patch_test.go), testServerWithDB and
  seedUser (server_test.go), doJSON (channels_test.go), openTestDB (store).
  Check before adding one; a redeclaration costs a build-fail round trip.
- The API depends on the checker pipeline only through small interfaces on
  Server (bus, prober), each nil-tolerant. Keep that pattern for new
  dependencies — it is what keeps the short suite off the network.
