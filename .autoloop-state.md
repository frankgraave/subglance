# SubGlance — autonomous work loop handoff

This file is the memory between runs. Each run reads it first and rewrites it
last. It exists because a cron run starts with an empty context: without this,
every run would spend its first (and most expensive) calls rediscovering where
the project stands.

Keep it SHORT. It is read on every run, so length here is a recurring cost.
Aim for under 60 lines. Delete anything that is no longer true rather than
appending to it — this is a current-state file, not a log. The log lives in
Linear comments and git history, which are the durable record.

---

## Current state

Last updated: 2026-09-10 09:20 UTC
Last completed: SUB-14 (heartbeat retention rollup) — commit f6a4884
Branch: main, working tree clean, everything pushed

Phase 1 backend is complete. Done: skeleton (SUB-8), database (SUB-9), HTTP
checker (SUB-10), TCP/ping/SSL (SUB-11), scheduler (SUB-12), state engine
(SUB-13), retention rollup (SUB-14), auth (SUB-16), SSE (SUB-17), SSRF
protection (SUB-18). CI covers race, lint, three-platform build, govulncheck.

Schema is at migration 0003. Rollup lives in internal/store/retention.go and
runs daily from cmd/subglance/main.go (also once at startup).

## In progress

Nothing. Next run picks a fresh issue.

## Next up (my judgement, not fixed)

1. SUB-15 — remaining API endpoints + OpenAPI spec. Urgent, backend, the last
   real gap before the frontend can be built against something stable.
2. SUB-20 — design tokens. Values already decided in docs/DESIGN.md §2; copy
   them, do not re-invent them. Blocks SUB-46/47.
3. SUB-46/47 — dashboard states (stale connection, paused vs no-data). Both
   are designed in docs/DESIGN.md and mocked in docs/mockups/. They need the
   frontend from SUB-20 to exist first.

## Standing constraints

- Go directive stays at 1.25.0. CI pins Go 1.25.14 and both linter versions;
  bumping the directive means checking a golangci-lint release exists that was
  built with that Go.
- Short test suite must stay hermetic: `go test -short` may not touch the
  network. Network tests sit behind SUBGLANCE_TEST_NETWORK=1.
- Never `git push --force`, never rewrite history.
- Commit messages in Dutch. Code, comments and docs in English.
- Fix lint findings; do not suppress them.
- golangci-lint under `systemd-run --scope -p MemoryMax=1500M`: the host has
  3.7GB and an unbounded lint run has taken the gateway down before.

## Hard-won lessons (do not relearn these)

- Long sessions cost ~3.3x per call versus short ones. Finish one issue, stop.
- A failing test is a hypothesis, not a nuisance. Three "broken tests" so far
  were all real product bugs.
- Verify a fix by breaking it — and take the result seriously. This run, one of
  three sabotage checks stayed green: the boundary test had picked a value
  outside the window the fix governs. Writing the test is not the proof; seeing
  it fail without the fix is.
- Aggregates need their sample count stored, or they cannot be merged later.
  heartbeat_hourly needed a latency_count column added after the fact.
