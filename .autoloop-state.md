# SubGlance — autonomous work loop handoff

This file is the memory between runs. Each run reads it first and rewrites it
last. It exists because a cron run starts with an empty context: without this,
every run would spend its first (and most expensive) calls rediscovering where
the project stands.

Keep it SHORT. It is read on every run, so length here is a recurring cost.
Aim for under 60 lines. Delete anything that is no longer true rather than
appending to it — this is a current-state file, not a log. The log lives in
Linear comments and git history, which are the durable record.

---

## Current state

Last updated: 2026-09-10 09:05 UTC
Last completed: SUB-17 (SSE live updates) — commit 49418c5, CI green
Branch: main, working tree clean, everything pushed

Phase 1 is nearly complete. Done: project skeleton (SUB-8), database (SUB-9),
HTTP checker (SUB-10), scheduler (SUB-12), state engine (SUB-13), TCP/ping/SSL
checkers (SUB-11), SSRF protection with bypass tests (SUB-18), authentication
(SUB-16), SSE stream (SUB-17). CI covers race detection, lint, three-platform
cross-compile, and vulnerability scanning.

## In progress

Nothing. Next run picks a fresh issue.

## Next up (my judgement, not fixed)

1. SUB-14 — heartbeat retention aggregation. Self-contained, backend, testable.
2. SUB-15 — remaining API endpoints + OpenAPI spec.
3. SUB-20 — design tokens. Values are already decided in docs/DESIGN.md §2;
   copy them, do not re-invent them.

## Standing constraints

- Go directive stays at 1.25.0. CI pins Go 1.25.14 and both linter versions;
  bumping the directive means checking a golangci-lint release exists that was
  built with that Go.
- Short test suite must stay hermetic: `go test -short` may not touch the
  network. Network tests sit behind SUBGLANCE_TEST_NETWORK=1.
- Never `git push --force`, never rewrite history.
- No new paid services or dependencies with licence implications.
- Commit messages in Dutch. Code, comments and docs in English.
- Fix lint findings; do not suppress them.

## Hard-won lessons (do not relearn these)

- Long sessions cost ~3.3x per call versus short ones, because every call drags
  the accumulated context. Finish one issue and stop. Do not chain work.
- A failing test is a hypothesis, not a nuisance. The last two "broken tests"
  were both real product bugs: a new monitor waiting a full interval for its
  first check, and the API silently accepting unknown JSON fields.
- Verify a fix by breaking it. If disabling the fix does not fail the test, the
  test proves nothing.
- Independent review finds interaction bugs that unit tests miss. Seven real
  bugs came out of it, none in a single function.
- The reviewer that ran against a working tree I was editing reported false
  findings. Measure claims before acting on them.

## Open questions for Frank

None right now.
